# 대출/정책자금 안내문 자동 생성기 — 스펙

## Metadata
- 작성일: 2026-04-29
- 인터뷰 라운드: 1 (4문항 일괄)
- 프로젝트 유형: greenfield
- 상태: 구현 완료

## Goal
재무제표 PDF를 업로드하면 핵심 재무 항목(업종, 매출액, 부채비율, 이자보상비율, 차입금 총잔액)을 자동 추출하고, 사용자가 결과 유형(보증기관 추가 검토 / 정책자금 부결 / 정책자금 승인) 중 하나를 선택하면 해당 안내문 본문을 자동 작성·다운로드할 수 있는 Streamlit 웹 앱.

## 사용자 결정 사항 (인터뷰 응답)
| 항목 | 선택 |
|------|------|
| 플랫폼 | Python + 웹 UI (Streamlit) |
| 입력 형식 | PDF 재무제표 업로드 |
| 결정 로직 | 사용자가 결과 선택 + 본문 자동 작성 |
| 임계값 처리 | 재무제표 PDF에서 추출된 수치를 그대로 사용 (자동 판정 없음) |

## Constraints
- 한국 재무제표 양식(원/천원/백만원/억원 단위)을 자동 감지·환산
- PDF 양식이 회사/기관마다 달라 100% 자동 추출은 불가 → 모든 추출값을 사용자가 화면에서 직접 수정 가능해야 함
- 안내문 원문(특수문자 ━ ▶ ✔ ✘ ⚠ ■ 必 등)은 절대 변경하지 말 것
- 재무제표 외에 입력해야 하는 항목(자금명, 지점명, 승인금액, 부결사유, 향후전략)은 UI에서 받음

## Non-Goals
- PDF 추출이 빗나갔을 때 OCR 기반 보정 (v1은 텍스트 PDF 가정)
- 재무비율 임계값을 두고 결과 자동 판정 (사용자가 결과 선택)
- DB 저장, 사용자 계정, 이력 관리
- 한컴오피스 한글(.hwp), 이미지 PDF 지원

## Acceptance Criteria
- [x] `streamlit run app.py`로 웹 앱이 기동되고 health 200 응답
- [x] PDF 업로드 → "PDF에서 값 추출" 버튼 클릭 시 업종·매출액·부채총계·자본총계·영업이익·이자비용·단기차입금·장기차입금이 화면 폼에 채워짐
- [x] 단위(원/천원/백만원/억원)가 PDF 내 "단위:" 표기에서 자동 감지되며 사용자가 변경 가능
- [x] 부채비율, 이자보상비율, 차입금 총잔액이 입력값 기반으로 자동 산출되어 메트릭으로 표시됨
- [x] 라디오 버튼으로 3가지 결과 유형 중 하나 선택 가능
- [x] 보증기관 추가 검토: 지점명만 입력해도 안내문 생성됨
- [x] 정책자금 부결: 자금명, 부결사유, 향후전략 입력 가능 (재무수치는 자동 채움). 부결사유/전략은 기본값(부채비율 과다 및 이자보상비율 1배 미만…)이 미리 들어감
- [x] 정책자금 승인: 자금명, 승인금액 입력 시 안내문 생성
- [x] 생성 결과는 textarea로 보여 복사 가능, `.txt` 다운로드 버튼 제공
- [x] 안내문 원문의 특수문자/구분선/금기사항 표기가 그대로 유지됨

## 자동 산출 지표
- 부채비율 = 부채총계 / 자본총계 × 100
- 이자보상비율 = 영업이익 / 이자비용
- 차입금 총잔액 = 단기차입금 + 장기차입금

## 파일 구성
- `app.py` — Streamlit UI (업로드 → 폼 수정 → 결과 선택 → 안내문 출력)
- `pdf_parser.py` — pdfplumber + 정규식 기반 추출, 단위 감지, 한국식 금액 포맷팅
- `templates.py` — 3가지 안내문 템플릿 + 부결 사유/전략 기본값
- `requirements.txt` — streamlit, pdfplumber
- `README.md` — 설치/실행/사용 흐름

## 안내문 템플릿 키 항목
| 결과 유형 | 사용자 입력 | 자동 채움 |
|----------|-------------|-----------|
| 보증기관 추가 검토 | 지점명 | (없음) |
| 정책자금 부결 | 자금명, 부결사유, 향후전략 | 업종, 매출액, 부채비율, 이자보상비율, 차입금 총잔액 |
| 정책자금 승인 | 자금명, 승인금액 | (없음) |

## 검증 결과
- 3개 모듈 구문 검사 통과
- 3가지 템플릿 모두 합성 데이터로 정상 렌더링 확인
- PDF 파서 헬퍼 단위 테스트 (단위 감지, 숫자 파싱(쉼표/괄호/△/공백), 라벨 검색, 부채비율·이자보상비율·차입금 합 산출, 한국식 금액 포맷팅) 모두 통과
- Streamlit 서버 기동 후 `/_stcore/health` HTTP 200 OK
- ⚠️ 실제 재무제표 PDF로 종단 검증은 미진행. 회사/기관별 PDF 양식 차이로 추출 실패 발생 가능 → 모든 필드를 사용자가 폼에서 수정 가능하도록 구현

## 향후 보강 가능 항목
- 한국 재무제표 양식별 표 파싱 정확도 향상 (pdfplumber.extract_tables() 활용)
- 단기/장기차입금 세부 명세(은행·잔액)를 표로 추출
- 결과 유형별 임계값 자동 판정 옵션 추가 (부채비율 ≥ 400% AND 이자보상비율 < 1.0 → 부결 추천 등)
- 한컴 한글 또는 이미지 PDF에 대한 OCR 보강
